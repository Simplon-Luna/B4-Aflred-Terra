#!/bin/bash

for i in {1..5}
do
    curl -F 'vote=Dogs' http://votingapp-b4g4ld-rg.eastus.cloudapp.azure.com/
    curl -F 'vote=Cats' http://votingapp-b4g4ld-rg.eastus.cloudapp.azure.com/

done


# curl --silent --output /dev/null -F 'vote=Cats' http://votingapp-b4g4ld-rg.eastus.cloudapp.azure.com
#    sleep 0.5